import { requireUser } from "@/lib/session"; import { prisma } from "@/lib/prisma"; import Link from "next/link"; export default async function ActivityPage(){const u=await requireUser();const items=await prisma.activity.findMany({where:{userId:u.id},orderBy:{createdAt:"desc"},take:100});return <main className="page-shell"><div className="mx-auto max-w-4xl"><Link href="/dashboard" className="text-sm text-black/45">← Dashboard</Link><h1 className="mt-6 text-4xl font-semibold">Activity</h1><div className="flow-card mt-8 rounded-3xl p-5 divide-y divide-black/6">{items.length?items.map(a=><div key={a.id} className="flex gap-3 py-4"><span className="mt-2 size-1.5 shrink-0 rounded-full bg-black/25"/><div><div className="text-sm">{a.message}</div><div className="mt-1 text-xs text-black/35">{new Date(a.createdAt).toLocaleString()}</div></div></div>):<div className="p-8 text-center text-sm text-black/45">Your timeline will appear here.</div>}</div></div></main>}
