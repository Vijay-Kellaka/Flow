"use client";
import Link from "next/link";
import { useState } from "react";

export default function RegisterPage(){const [name,setName]=useState("");const[email,setEmail]=useState("");const[pwd,setPwd]=useState("");const[msg,setMsg]=useState("");const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const r=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,email,password:pwd})});const d=await r.json();setBusy(false);setMsg(r.ok?'Check your email to verify your Flow account.':d.error||'Could not create account.')}return <main className="flex min-h-screen items-center justify-center px-6"><div className="w-full max-w-md"><Link href="/" className="text-sm text-black/40">← Flow</Link><div className="mt-8 text-center"><h1 className="text-3xl font-semibold">Create your space.</h1><p className="mt-2 text-sm text-black/45">Your data stays tied to your account.</p></div><form onSubmit={submit} className="flow-card mt-7 space-y-3 rounded-3xl p-6"><input className="flow-input" value={name} onChange={e=>setName(e.target.value)} placeholder="Name" required/><input className="flow-input" value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="Email" required/><input className="flow-input" value={pwd} onChange={e=>setPwd(e.target.value)} type="password" placeholder="Password (8+ characters)" required/><button disabled={busy} className="w-full rounded-2xl bg-black px-4 py-3 text-sm font-medium text-white">{busy?'Creating…':'Create account'}</button>{msg&&<p className="text-xs leading-5 text-black/45">{msg}</p>}</form><div className="mt-4 text-center text-xs text-black/40">Already have an account? <Link href="/login" className="underline">Sign in</Link></div></div></main>}
